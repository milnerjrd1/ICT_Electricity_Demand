scenario_id: ai_stress
label: "AI Stress"
family: ai_dc

pue_improvement_rate: 0.005       # Minimal PUE improvement — demand far outpaces efficiency
utilisation_multiplier: 1.35      # Very high utilisation — capacity fully saturated
ai_growth_rate: 0.80              # Extreme AI compute growth (80% annual) — AGI-era buildout
ai_kwh_per_eflop: 0.0018          # Less efficient per EFLOP — brute-force scaling

avg_lifespan_multiplier: 0.85     # Fast device refresh — AI-capable hardware obsoletes quickly

power_efficiency_factor: 1.15     # Network equipment severely strained by traffic surge

placement_shares:
  hyperscale: 0.55
  sovereign: 0.20
  colocation: 0.18
  on_premises: 0.05
  edge: 0.02

placement_share_delta:
  hyperscale: 0.03
  sovereign: 0.015
  colocation: -0.02
  on_premises: -0.02
  edge: -0.005
